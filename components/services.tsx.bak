"use client"

import Link from "next/link"
import PageTransition from "@/components/page-transition"
import { DynamicIcon } from "@/lib/dynamic-icon"
import { arcgisDepartment, itDepartment, oceanicDepartment } from "@/lib/data"
import type { Layanan } from "@/lib/database.types"

interface ServicesProps {
  arcgisLayanan: Layanan[]
  itLayanan: Layanan[]
}

export default function Services({ arcgisLayanan, itLayanan }: ServicesProps) {
  return (
    <section className="py-24 bg-white" id="services">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">

        {/* Section header */}
        <div className="text-center mb-20">
          <h2 className="text-3xl md:text-4xl font-bold text-black mb-4">Layanan Kami</h2>
          <p className="text-black/50 text-lg max-w-2xl mx-auto">
            Tiga departemen spesialis, satu misi — solusi teknis yang nyata untuk pemetaan lingkungan, teknologi digital, dan desain kapal.
          </p>
        </div>

        {/* ── DEPARTEMEN TEKNIK LINGKUNGAN ── */}
        <PageTransition delay={100}>
          <div className="mb-20">
            <div className="flex items-center gap-4 mb-10">
              <div className="flex items-center gap-3">
                <div className="w-10 h-10 rounded-xl bg-[#ff914d] flex items-center justify-center">
                  <svg className="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 20l-5.447-2.724A1 1 0 013 16.382V5.618a1 1 0 011.447-.894L9 7m0 13l6-3m-6 3V7m6 10l4.553 2.276A1 1 0 0021 18.382V7.618a1 1 0 00-.553-.894L15 4m0 13V4m0 0L9 7" />
                  </svg>
                </div>
                <div>
                  <div className="text-xs font-bold text-[#ff914d] uppercase tracking-widest">Departemen</div>
                  <h3 className="text-xl font-bold text-black">{arcgisDepartment.title}</h3>
                </div>
              </div>
              <div className="flex-1 h-px bg-black/8" />
            </div>
            <p className="text-black/50 text-base mb-8 max-w-2xl">{arcgisDepartment.subtitle}</p>

            <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-5">
              {(arcgisLayanan.length > 0 ? arcgisLayanan : arcgisDepartment.services).map((service) => (
                <Link
                  key={service.slug ?? service.href}
                  href={"slug" in service ? `/services/${service.slug}` : service.href}
                  className="group bg-white rounded-2xl p-6 border border-black/8 hover:border-[#ff914d]/40 hover:shadow-xl hover:shadow-orange-100 transition-all duration-300 hover:-translate-y-1 relative overflow-hidden"
                >
                  <div className="absolute bottom-0 left-0 h-0.5 w-0 group-hover:w-full bg-[#ff914d] transition-all duration-500" />
                  <div className="w-12 h-12 rounded-xl bg-[#ff914d]/10 flex items-center justify-center mb-5 group-hover:bg-[#ff914d]/20 transition-colors duration-300">
                    <DynamicIcon name={"icon" in service ? (service.icon ?? "map") : "map"} color="#ff914d" size={24} />
                  </div>
                  <h4 className="text-base font-bold text-black mb-2 group-hover:text-[#ff914d] transition-colors duration-200">
                    {service.title}
                  </h4>
                  <p className="text-black/50 text-sm leading-relaxed">{service.description}</p>
                  <div className="mt-4 flex items-center gap-1 text-[#ff914d] text-sm font-semibold opacity-0 group-hover:opacity-100 transition-opacity duration-300">
                    Selengkapnya
                    <svg className="w-3.5 h-3.5 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 5l7 7-7 7" />
                    </svg>
                  </div>
                </Link>
              ))}
            </div>
          </div>
        </PageTransition>

        {/* ── DEPARTEMEN IT & DIGITAL ── */}
        <PageTransition delay={200}>
          <div className="mb-20">
            <div className="flex items-center gap-4 mb-10">
              <div className="flex items-center gap-3">
                <div className="w-10 h-10 rounded-xl bg-black flex items-center justify-center">
                  <svg className="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4" />
                  </svg>
                </div>
                <div>
                  <div className="text-xs font-bold text-black/40 uppercase tracking-widest">Departemen</div>
                  <h3 className="text-xl font-bold text-black">{itDepartment.title}</h3>
                </div>
              </div>
              <div className="flex-1 h-px bg-black/8" />
            </div>
            <p className="text-black/50 text-base mb-8 max-w-2xl">{itDepartment.subtitle}</p>

            <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-5">
              {(itLayanan.length > 0 ? itLayanan : itDepartment.services).map((service) => (
                <Link
                  key={service.slug ?? service.href}
                  href={"slug" in service ? `/services/${service.slug}` : service.href}
                  className="group bg-white rounded-2xl p-6 border border-black/8 hover:border-black/25 hover:shadow-xl hover:shadow-black/5 transition-all duration-300 hover:-translate-y-1 relative overflow-hidden"
                >
                  <div className="absolute bottom-0 left-0 h-0.5 w-0 group-hover:w-full bg-black transition-all duration-500" />
                  <div className="w-12 h-12 rounded-xl bg-black/5 flex items-center justify-center mb-5 group-hover:bg-black/10 transition-colors duration-300">
                    <DynamicIcon name={"icon" in service ? (service.icon ?? "code") : "code"} color="#111111" size={24} />
                  </div>
                  <h4 className="text-base font-bold text-black mb-2 group-hover:text-black transition-colors duration-200">
                    {service.title}
                  </h4>
                  <p className="text-black/50 text-sm leading-relaxed">{service.description}</p>
                  <div className="mt-4 flex items-center gap-1 text-black text-sm font-semibold opacity-0 group-hover:opacity-100 transition-opacity duration-300">
                    Selengkapnya
                    <svg className="w-3.5 h-3.5 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 5l7 7-7 7" />
                    </svg>
                  </div>
                </Link>
              ))}
            </div>
          </div>
        </PageTransition>

        {/* ── DEPARTEMEN TEKNIK KELAUTAN ── */}
        <PageTransition delay={300}>
          <div>
            <div className="flex items-center gap-4 mb-10">
              <div className="flex items-center gap-3">
                <div className="w-10 h-10 rounded-xl bg-[#0a6e8a] flex items-center justify-center">
                  <svg className="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M3 17l3-8 4 4 4-6 3 5M3 21h18" />
                  </svg>
                </div>
                <div>
                  <div className="text-xs font-bold text-[#0a6e8a] uppercase tracking-widest">Departemen</div>
                  <h3 className="text-xl font-bold text-black">{oceanicDepartment.title}</h3>
                </div>
              </div>
              <div className="flex-1 h-px bg-black/8" />
            </div>
            <p className="text-black/50 text-base mb-8 max-w-2xl">{oceanicDepartment.subtitle}</p>

            <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-5">
              {oceanicDepartment.services.map((service) => (
                <Link
                  key={service.href}
                  href={service.href}
                  className="group bg-white rounded-2xl p-6 border border-black/8 hover:border-[#0a6e8a]/40 hover:shadow-xl hover:shadow-cyan-100 transition-all duration-300 hover:-translate-y-1 relative overflow-hidden"
                >
                  <div className="absolute bottom-0 left-0 h-0.5 w-0 group-hover:w-full bg-[#0a6e8a] transition-all duration-500" />
                  <div className="w-12 h-12 rounded-xl bg-[#0a6e8a]/10 flex items-center justify-center mb-5 group-hover:bg-[#0a6e8a]/20 transition-colors duration-300">
                    <DynamicIcon name={service.icon} color="#0a6e8a" size={24} />
                  </div>
                  <h4 className="text-base font-bold text-black mb-2 group-hover:text-[#0a6e8a] transition-colors duration-200">
                    {service.title}
                  </h4>
                  <p className="text-black/50 text-sm leading-relaxed">{service.description}</p>
                  <div className="mt-4 flex items-center gap-1 text-[#0a6e8a] text-sm font-semibold opacity-0 group-hover:opacity-100 transition-opacity duration-300">
                    Selengkapnya
                    <svg className="w-3.5 h-3.5 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 5l7 7-7 7" />
                    </svg>
                  </div>
                </Link>
              ))}
            </div>
          </div>
        </PageTransition>

      </div>
    </section>
  )
}
